// import React from 'react'
// import { useFarmData } from '../../mockData';

// function Filterbutton() {
//     const { data, isLoading, error } = useFarmData();

//     const dataFilter = ()=>{
//         const result= data.sort((yield1, yield2)=>{
//             return (yield1.yield<yield2.yield) ? 1 : (yield1.yield >yield2.yield) ?-1: 0
           
//         })
// console.log(result)
//     }

//   return (
//     <div><button onClick={dataFilter}>Sort Data</button>Filterbutton</div>
//   )
// }

// export default Filterbutton
